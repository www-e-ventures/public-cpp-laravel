# Build output
/build/
build*/

# CMake artifacts (in case of in-source configure)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile

# Compiled binaries / objects (executables live in build/, already ignored)
*.o
*.a
*.out

# Parity oracle (PHP) — installed deps and the scratch sqlite db
parity/vendor/
parity/.phpunit.cache/
parity/.phpunit.result.cache
*.sqlite

# Release-prep runbook + dev notes — not part of the codebase
humanisation/

# Editor / OS cruft
.vscode/
.idea/
*.swp
.DS_Store

# WebAssembly build output
wasm/dist/

# FFI build output
ffi/dist/
